void main() {
  // secara default, semua var harus isi data,
  // jika tidak maka tidak bisa digunakan
  // agar bisa diisi dengan null, maka dikasih tanda tanya
  // nullable
  
  // String? prestasi = null;
  String? prestasi;
  print(prestasi);
}
